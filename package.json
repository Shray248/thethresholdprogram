{
  "name": "threshold-program",
  "version": "1.0.0",
  "scripts": {
    "build": "exit 0",
    "vercel-build": "echo 'No build required'",
    "dev": "npx serve . -p 3000"
  },
  "dependencies": {
    "puppeteer": "^25.1.0"
  }
}
